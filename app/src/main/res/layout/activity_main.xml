<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/etNombre"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/etContraseña"
        android:hint="@string/nombre"
        android:layout_marginBottom="10dp"> </EditText>

    <EditText
        android:id="@+id/etContraseña"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/btLogin"
        android:layout_marginBottom="30dp"
        android:hint="@string/contraseña"> </EditText>

    <Button
        android:id="@+id/btLogin"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/login"
        tools:ignore="MissingConstraints"
        android:onClick="onClickLogin"> </Button>

    <Button
        android:id="@+id/btRegister"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/btLogin"
        android:layout_centerInParent="true"
        tools:ignore="MissingConstraints"
        android:text="@string/registro"
        android:onClick="onClickRegister"> </Button>

</RelativeLayout>